/** Shared Mantine field styling for PropertyPanel inputs */
export const FIELD_STYLES = {
  input: { fontSize: 12 },
  label: { fontSize: 11, fontWeight: 500 },
} as const

export const MONO_INPUT = {
  input: { fontSize: 12, fontFamily: 'var(--font-mono)' },
  label: { fontSize: 11, fontWeight: 500 },
} as const
